function searchInfo() {
    alert("Search feature will be implemented in future version.");
}

function changeLanguage(language) {
    if (language === "en") {
        alert("English selected.");
    } else if (language === "zh") {
        alert("Chinese selected. Full translation will be available in a future version.");
    } else if (language === "ar") {
        alert("Arabic selected. Full translation will be available in a future version.");
    } else if (language === "hi") {
        alert("Hindi selected. Full translation will be available in a future version.");
    } else if (language === "vi") {
        alert("Vietnamese selected. Full translation will be available in a future version.");
    }
}